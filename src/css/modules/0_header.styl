// override
body.admin-menu.adminimal-menu:before
  display: none

.header
  height: 380px
  position: relative
  background-repeat: no-repeat
  background-position: center bottom

.header__background
  position: absolute
  left: 0
  width: 100%
  top: 0
  height: 100%
  z-index: 0
  background: url(../img/logo-bg.png) repeat-x

.header > .content-container
  position: relative
  z-index: 100

for $section-name in $section-colors
  .body-section-{$section-name} .header
    background-image: url(../img/banners/$section-name.jpg)

.header__logo-container
  margin-top: 40px

.header__nav
  margin-top: 15px

+below(1000px)
  .header__logo
    display: inline-block
    max-width: 80%
  // 非桌面时，header 高度与宽度成比例
  .header
    height: auto
    background-size: cover
    &::after
      // 16:9
      padding-top: unit(380/1050*100, '%')
      display: block
      content: ''
    .content-container
      position: absolute
      left: 0
      top: 0
      width: 100%
      height: 100%
  
// retina 下图片 2x
/*
+retina()
  .header__background
    background: url(../img/logo-bg@2x.png) repeat-x
*/
